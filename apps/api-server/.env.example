# Client Portal URL for cache revalidation
CLIENT_PORTAL_URL=http://localhost:4200

# Secret token for cache revalidation (must match client-portal's REVALIDATE_SECRET)
REVALIDATE_SECRET=your-secret-token-here
